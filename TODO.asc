* Testing!
* Make plugins optional via config file
** Either by omitting config sections or including a disabled="true" option
* Reminders
** Wire up reminders to other people (remind foo to ....)
** Determine where a reminder should be sent (same channel/protocol, everywhere, try to track down user, etc.)
*** This might even need to be configurable
* ClojureCLR plugin